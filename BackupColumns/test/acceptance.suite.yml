class_name: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - AcceptanceHelper
        - Db
    config:
      WebDriver:
         url: 'http://modules.gear.dev'
         browser: phantomjs
         capabilities:
             webStorageEnabled: true
      Db:
          dsn: 'mysql:dbname=pibernews;host=localhost'
          user: 'root'
          password: 'gear'
          dump: data/column.mysql.sql