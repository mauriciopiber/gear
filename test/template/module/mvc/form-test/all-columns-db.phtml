<?php
namespace MyModuleTest\FormTest;

use GearBaseTest\AbstractTestCase;

/**
 * @group MyModule
 * @group Table
 * @group Form
 */
class TableTest extends AbstractTestCase
{
    public function getTable()
    {
        if (!isset($this->table)) {
            $this->table = $this->bootstrap->getServiceLocator()->get(
                'MyModule\Form\Table'
            );
        }
        return $this->table;
    }

    /**
     * @group MyModule
     * @group Table
     */
    public function testServiceLocator()
    {
        $serviceLocator = $this->getTable()->getServiceLocator();
        $this->assertInstanceOf('Zend\ServiceManager\ServiceManager', $serviceLocator);
    }

    /**
     * @group MyModule
     * @group Table
     */
    public function testCallUsingServiceLocator()
    {
        $table = $this->getTable();
        $this->assertInstanceOf('MyModule\Form\Table', $table);
    }
}
