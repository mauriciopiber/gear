#!/bin/bash

bin="$( cd "$( dirname "${BASH_SOURCE[0***REMOVED***}" )" && cd ./../../../../../bin && pwd )"
scriptDir="$( cd "$( dirname "${BASH_SOURCE[0***REMOVED***}" )" && pwd )"

project="PersonalResourcePlanner"

modules=""
modulesClear=""

source "$bin/gear/project/test-project.sh"
	
