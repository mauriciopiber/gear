#!/bin/bash

ssh -o "StrictHostKeyChecking no" deployer@my-project.stag01.pibernetwork.com << EOF
    cd /var/www/staging/my-project
    sudo git pull
    sudo composer install -o
    export PHINX_ENVIRONMENT=STAGING
    sudo vendor/bin/phinx migrate
EOF
