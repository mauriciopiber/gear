<target name="phpcs-namespace" description="Code Sniffer">
    <exec executable="vendor/bin/phpcs">
        <arg value="--standard=test/phpcs.xml"/>
        <arg path="${basedir}/src/${namespace}"/>
    </exec>
</target>