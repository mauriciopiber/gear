namespace <?php echo $this->namespace;?>;

use Interop\Container\ContainerInterface;
use GearBase\Hydrator\DateHydrator;
use GearBase\Factory\AbstractFactory;
use <?php echo $this->namespace;?>\<?php echo $this->class;?>;
use <?php echo $this->filter;?>;
use <?php echo $this->entity;?>;

/**
 * PHP Version 5
 *
 * @category Factory
 * @package <?php echo $this->package.PHP_EOL;?>
 * @author Mauricio Piber <mauriciopiber@gmail.com>
 * @license GPL3-0 http://www.gnu.org/licenses/gpl-3.0.en.html
 * @link http://pibernetwork.com
 */
class <?php echo $this->class;?>Factory extends AbstractFactory
{

    /**
     * Create <?php echo $this->class.PHP_EOL;?>
     *
     * @param ContainerInterface $container ServiceManager instance
     *
     * @return <?php echo $this->class.PHP_EOL;?>
     */
    public function __invoke(ContainerInterface $container, $requestedName = null, $options = [***REMOVED***)
    {
        $form = new <?php echo $this->class;?>(
            $container->get('doctrine.entitymanager.orm_default')
        );

        $<?php echo $this->var;?> = null;
<?php echo $this->setId;?>

        $filter = $container->get(<?php echo $this->filterName;?>::class);
        $form->setInputFilter($filter->getInputFilter($<?php echo $this->var;?>));

        $entityManager = $container->get('doctrine.entitymanager.orm_default');
        $hydrator = new DateHydrator($entityManager, <?php echo $this->entityName;?>::class);

        $form->setHydrator($hydrator);

        return $form;
    }
}
