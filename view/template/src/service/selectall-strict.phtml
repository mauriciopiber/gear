
        if ($this->getAuthService()->hasIdentity()) {
            $select = array_merge(
                $select,
                array('createdBy' => $this->getAuthService()->getIdentity()->getId())
            );
        }
        return parent::selectAll($select);
