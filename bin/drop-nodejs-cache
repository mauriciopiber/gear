#!/bin/bash

nodepath="/tmp/node_modules"

ls -l $nodepath &> /dev/null

if [ "$?" == "0" ***REMOVED***; then

    sudo rm -R $nodepath

    echo "Cache NodeJS Removido com sucesso"
    
    exit 1
fi


echo "Cache NodeJS Não encontrado"